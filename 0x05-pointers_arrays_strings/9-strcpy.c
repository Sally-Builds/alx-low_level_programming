#include <string.h>
#include <stdio.h>
/**
 * _strcpy - function to copy strings
 * @desc: destination to cpy
 * @src: source to copy from
 *
 * Return: copied string
 */
char *_strcpy(char *desc, char *src)
{
	return (strcpy(desc, src));
}
